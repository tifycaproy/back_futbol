container_commands:
    01-clean-nginx-files:
        command: "sudo rm -rf *_app_server.* && sudo rm -rf _proxy.*"
    02-install-nginx:
        command: "rpm -qa | grep -qw nginx || sudo yum install nginx -y"